import * as React from 'react';
import { ButtonProps, Button } from '@mui/material';

export const DenButton: React.FC<ButtonProps> = (props) => (
  <Button {...props} />
);

export default DenButton;
