import * as React from 'react';
import { FormLabelProps, FormLabel } from '@mui/material';

export const DenFormLabel: React.FC<FormLabelProps> = (props) => (
  <FormLabel {...props} />
);

export default DenFormLabel;
