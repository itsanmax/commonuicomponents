import * as React from 'react';
import { DialogProps, Dialog } from '@mui/material';

export const DenDialog: React.FC<DialogProps> = (props) => (
  <Dialog {...props} />
);

export default DenDialog;
