import * as React from 'react';
import { TypographyProps, Typography } from '@mui/material';

export const DenTypography: React.FC<TypographyProps> = (props) => (
  <Typography {...props} />
);

export default DenTypography;
