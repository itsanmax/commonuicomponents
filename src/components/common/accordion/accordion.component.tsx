import * as React from 'react';
import { AccordionProps, Accordion } from '@mui/material';

export const DenAccordion: React.FC<AccordionProps> = (props) => (
  <Accordion {...props} />
);

export default DenAccordion;
