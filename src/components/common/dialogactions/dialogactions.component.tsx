import * as React from 'react';
import { DialogActionsProps, DialogActions } from '@mui/material';

export const DenDialogActions: React.FC<DialogActionsProps> = (props) => (
  <DialogActions {...props} />
);

export default DenDialogActions;
