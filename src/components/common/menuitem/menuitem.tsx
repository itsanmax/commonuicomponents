import * as React from 'react';
import { MenuItem, MenuItemProps } from '@mui/material';

export const DenMenuItem: React.FC<MenuItemProps> = (props) => (
  <MenuItem {...props} />
);

export default DenMenuItem;
