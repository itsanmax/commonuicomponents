import * as React from 'react';
import { TooltipProps, Tooltip } from '@mui/material';

export const DenTooltip: React.FC<TooltipProps> = (props) => (
  <Tooltip {...props} />
);

export default DenTooltip;
