import * as React from 'react';
import { DialogContentProps, DialogContent } from '@mui/material';

export const DenDialogContent: React.FC<DialogContentProps> = (props) => (
  <DialogContent {...props} />
);

export default DenDialogContent;
