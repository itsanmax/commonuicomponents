import * as React from 'react';
import { DialogTitleProps, DialogTitle } from '@mui/material';

export const DenDialogTitle: React.FC<DialogTitleProps> = (props) => (
  <DialogTitle {...props} />
);

export default DenDialogTitle;
